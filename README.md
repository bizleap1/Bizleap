# Next.js + Tailwind CSS Boilerplate

Minimal stable starter with:
- Next.js (pages router)
- Tailwind CSS (PostCSS + Autoprefixer)

Quick start:

1. Install deps:
   ```bash
   npm install
   # or
   # yarn
   ```
2. Run dev server:
   ```bash
   npm run dev
   ```

That's it — a tiny, stable baseline to build from.
